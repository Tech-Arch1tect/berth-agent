# Berth Agent Configuration

# REQUIRED: Authentication token for API access
# Generate a secure token with: openssl rand -base64 32
ACCESS_TOKEN=your-secure-access-token-here

# Server Configuration
PORT=8080

# Stack Configuration
# Directory to scan for Docker Compose stacks
STACK_LOCATION=/opt/compose

# Audit Logging Configuration
AUDIT_LOG_ENABLED=false
AUDIT_LOG_FILE_PATH=/var/log/berth-agent/audit.jsonl
AUDIT_LOG_SIZE_LIMIT_MB=100

# Vulnerability Scanning Configuration
VULNSCAN_PERSISTENCE_DIR=/var/lib/berth-agent/scans